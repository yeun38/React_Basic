import React from "react"; // 리액트 패키지에서 리액트 객체 가져온다

const BucketList = (props) => {
  return <div>버킷 리스트</div>;
};

export { BucketList };
export default BucketList; // 다른 파일에서 해당 컴포넌트를 불러서 사용할 수 있도록 함.
